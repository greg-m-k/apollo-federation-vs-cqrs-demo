# GraphQL Schema: Quarkus Apollo GraphQL Demo
# Generated: 2025-12-21
# This schema is automatically generated by SmallRye GraphQL from Java annotations.
# This file serves as documentation and contract reference.

"""
A product category for grouping related products.
Demonstrates one-to-many relationships in GraphQL.
"""
type Category {
  """Unique identifier for the category"""
  id: ID!

  """Category name (e.g., "Electronics", "Books")"""
  name: String!

  """Optional description of the category"""
  description: String

  """List of products belonging to this category"""
  products: [Product!]!
}

"""
A product item that can be queried.
Core entity for demonstrating GraphQL queries and relationships.
"""
type Product {
  """Unique identifier for the product"""
  id: ID!

  """Product name"""
  name: String!

  """Optional product description"""
  description: String

  """Product price (must be >= 0)"""
  price: Float!

  """Category this product belongs to (optional)"""
  category: Category
}

"""
Root query type for the GraphQL API.
All read operations are defined here.
"""
type Query {
  """
  Get all products.
  Returns a list of all available products with their details.

  Example:
  ```graphql
  query {
    products {
      id
      name
      price
      category {
        name
      }
    }
  }
  ```
  """
  products: [Product!]!

  """
  Get a single product by ID.
  Returns null if the product is not found.

  Example:
  ```graphql
  query {
    product(id: "1") {
      name
      description
      price
    }
  }
  ```
  """
  product(id: ID!): Product

  """
  Get all categories.
  Returns a list of all product categories.

  Example:
  ```graphql
  query {
    categories {
      id
      name
      products {
        name
        price
      }
    }
  }
  ```
  """
  categories: [Category!]!

  """
  Get a single category by ID.
  Returns null if the category is not found.

  Example:
  ```graphql
  query {
    category(id: "1") {
      name
      description
      products {
        name
      }
    }
  }
  ```
  """
  category(id: ID!): Category
}
