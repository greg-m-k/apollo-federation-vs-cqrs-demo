server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # SPA routing - dashboard hits APIs directly via localhost ports
    location / {
        try_files $uri $uri/ /index.html;
    }
}
