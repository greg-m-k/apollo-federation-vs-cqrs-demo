apiVersion: v1
kind: Service
metadata:
  name: products-mutation
  namespace: apollo-demo
  labels:
    app: products-mutation
spec:
  selector:
    app: products-mutation
  ports:
    - port: 8080
      targetPort: 8080
      name: http
