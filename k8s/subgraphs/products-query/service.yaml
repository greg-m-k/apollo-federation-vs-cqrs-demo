apiVersion: v1
kind: Service
metadata:
  name: products-query
  namespace: apollo-demo
  labels:
    app: products-query
spec:
  selector:
    app: products-query
  ports:
    - port: 8080
      targetPort: 8080
      name: http
