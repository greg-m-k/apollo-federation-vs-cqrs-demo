apiVersion: apps/v1
kind: Deployment
metadata:
  name: hr-subgraph
  namespace: federation-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hr-subgraph
  template:
    metadata:
      labels:
        app: hr-subgraph
    spec:
      containers:
        - name: hr-subgraph
          image: hr-subgraph:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: DB_HOST
              value: postgres
            - name: DB_PORT
              value: "5432"
            - name: DB_NAME
              value: hr_db
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: POSTGRES_PASSWORD
          readinessProbe:
            httpGet:
              path: /q/health/ready
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /q/health/live
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: hr-subgraph
  namespace: federation-demo
spec:
  selector:
    app: hr-subgraph
  ports:
    - port: 8080
      targetPort: 8080
