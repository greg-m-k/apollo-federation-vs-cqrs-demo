apiVersion: v1
kind: Service
metadata:
  name: demo-dashboard
  namespace: apollo-demo
  labels:
    app: demo-dashboard
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30001
      protocol: TCP
      name: http
  selector:
    app: demo-dashboard
